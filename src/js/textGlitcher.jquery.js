(function ($) {
    $.fn.textGlitcher = function (options) {

        var settings = $.extend({
            autoplay: false,
        }, options);
        console.log(settings);

        return this.each(function () {

        });
    };
})(jQuery);